# Neural Core AI Threat Center - Environment Variables

# ==========================================
# EDGE SENSOR CONFIGURATION (Honeypot Nodes)
# ==========================================
SENSOR_ID=honeypot-edge-1
CONTROLLER_URL=http://<CONTROLLER_IP>:5000/receive_log
BLOCKLIST_SYNC_INTERVAL=30
LOG_RETRY_COUNT=3
LOG_TIMEOUT=5

# ==========================================
# CONTROLLER CONFIGURATION (Central Server)
# ==========================================
# Universal secure API key for edge-to-controller and controller-to-brain communication
API_KEY=your_super_secret_api_key_here

# Directory to store central logs and metrics inside the container
LOGS_DIR=/app/logs

# ==========================================
# BRAIN / AI ENGINE CONFIGURATION
# ==========================================
# Endpoint for the AI Brain Server (Usually Windows Host running Ollama/Mistral)
BRAIN_URL=http://<BRAIN_IP>:6001/analyze

# ==========================================
# DEPLOYMENT CONFIGURATION (Scripts)
# ==========================================
# IP Address or Hostname of the target Edge Device (e.g., Raspberry Pi)
PI_HOST=192.168.x.x
# SSH Credentials for deploying to the Edge Device
PI_USER=pi_username
PI_PASSWORD=pi_password
